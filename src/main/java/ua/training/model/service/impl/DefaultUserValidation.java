package ua.training.model.service.impl;

import ua.training.model.dto.InvalidData;
import ua.training.model.service.UserValidation;

public class DefaultUserValidation implements UserValidation {
    boolean isValid = true;
    InvalidData invalidData = new InvalidData();

    //todo make add here comparing user fields with regex

//    public InvalidData validate(User user) {
//        if(!user.getNameUa().isValid()){
//            invalidData.setAttributeUaName();
//        }








    }

